"""Vulture whitelist for dynamic framework and compatibility signatures.

Vulture cannot whitelist unused function parameters through this file alone, so
the workflow also passes these names via `--ignore-names`.
"""

stroke_width = object()
stroke_type = object()
page_number = object()
file_path = object()
preserve_format = object()
unit_count = object()
netclass_filter = object()
