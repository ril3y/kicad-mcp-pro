name: Trivy

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  merge_group:
  schedule:
    - cron: "15 7 * * 2"
  workflow_dispatch:
    inputs:
      kicad_appimage_url:
        description: "Optional KiCad 10 x86_64 AppImage URL for scanning Dockerfile.kicad10"
        required: false
        type: string

permissions:
  contents: read

jobs:
  scan:
    name: Trivy (base image)
    runs-on: ubuntu-latest
    timeout-minutes: 25
    permissions:
      contents: read
      security-events: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          persist-credentials: false
      - name: Build image
        run: docker build --pull -f Dockerfile -t kicad-mcp-pro:base .
      - name: Scan image vulnerabilities
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25
        with:
          image-ref: kicad-mcp-pro:base
          format: table
          scanners: vuln
          severity: HIGH,CRITICAL
          exit-code: "1"
          ignore-unfixed: true
          vuln-type: os,library
          version: v0.70.0
      - name: Generate SARIF
        if: always()
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25
        with:
          image-ref: kicad-mcp-pro:base
          format: sarif
          output: trivy-base.sarif
          scanners: vuln
          severity: HIGH,CRITICAL
          exit-code: "0"
          ignore-unfixed: true
          vuln-type: os,library
          limit-severities-for-sarif: true
          version: v0.70.0
      - uses: github/codeql-action/upload-sarif@e46ed2cbd01164d986452f91f178727624ae40d7
        if: always()
        with:
          sarif_file: trivy-base.sarif

  scan-kicad10:
    name: Trivy (KiCad 10 image)
    if: >
      github.repository == 'oaslananka-lab/kicad-mcp-pro' &&
      github.event_name == 'workflow_dispatch' &&
      inputs.kicad_appimage_url != ''
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
      security-events: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          persist-credentials: false
      - name: Build image
        env:
          KICAD_APPIMAGE_URL: ${{ inputs.kicad_appimage_url }}
        run: |
          docker build \
            --pull \
            --build-arg KICAD_APPIMAGE_URL="${KICAD_APPIMAGE_URL}" \
            -f Dockerfile.kicad10 \
            -t kicad-mcp-pro:kicad10 .
      - name: Scan image vulnerabilities
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25
        with:
          image-ref: kicad-mcp-pro:kicad10
          format: table
          scanners: vuln
          severity: HIGH,CRITICAL
          exit-code: "1"
          ignore-unfixed: true
          vuln-type: os,library
          version: v0.70.0
      - name: Generate SARIF
        if: always()
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25
        with:
          image-ref: kicad-mcp-pro:kicad10
          format: sarif
          output: trivy-kicad10.sarif
          scanners: vuln
          severity: HIGH,CRITICAL
          exit-code: "0"
          ignore-unfixed: true
          vuln-type: os,library
          limit-severities-for-sarif: true
          version: v0.70.0
      - uses: github/codeql-action/upload-sarif@e46ed2cbd01164d986452f91f178727624ae40d7
        if: always()
        with:
          sarif_file: trivy-kicad10.sarif
